name: Preview Deployment

on:
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  preview:
    uses: ./.github/workflows/vercel-preview-deploy.reusable.yml
    secrets: inherit
