# =============================================================================
# SUPABASE CONFIGURATION (PRIMARY DATABASE)
# =============================================================================
# 
# IMPORTANT: Environment Variable Naming Convention
# - Client-side (React app): Use VITE_SUPABASE_* prefix (required by Vite)
# - Server-side (API routes, serverless): Use SUPABASE_* prefix (without VITE_)
# 
# For Vercel/Production:
# - Set BOTH sets of variables in Vercel Environment Variables
# - Client-side: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
# - Server-side: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
#
# How to get these values:
# 1. Go to your Supabase project dashboard: https://supabase.com/dashboard
# 2. Select your project (e.g., supabase-teal-island)
# 3. In the left sidebar, click on "Settings" (gear icon)
# 4. Click on "API" in the Settings menu
# 5. You'll find three important values:
#    - Project URL: Copy the "Project URL" (e.g., https://xxxxx.supabase.co)
#    - anon public key: Copy the "anon public" key (safe to expose in client-side code)
#    - service_role key: Copy the "service_role" key (KEEP SECRET - only for server-side)
#
# =============================================================================
# CLIENT-SIDE VARIABLES (React/Vite - Required for frontend)
# =============================================================================

# Supabase Project URL (REQUIRED for client-side)
# Format: https://your-project-ref.supabase.co
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon Public Key (REQUIRED for client-side)
# This is safe to expose in client-side code - it's public by design
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =============================================================================
# SERVER-SIDE VARIABLES (API routes, serverless functions - Required for backend)
# =============================================================================
# Note: For serverless environments (Vercel), use SUPABASE_* (without VITE_ prefix)

# Supabase Project URL (REQUIRED for server-side)
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon Public Key (for server-side operations that use anon key)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (REQUIRED for admin operations - KEEP SECRET!)
# ⚠️ WARNING: This key bypasses Row Level Security (RLS) policies
# ⚠️ NEVER expose this in client-side code or commit it to version control
# ⚠️ Only use this in secure server-side environments
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# OTHER CONFIGURATION
# =============================================================================

# Gemini API Key (for AI features)
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Secret (for custom JWT tokens if needed)
JWT_SECRET=your_jwt_secret_here

# =============================================================================
# DEPRECATED: FIREBASE CONFIGURATION
# =============================================================================
# ⚠️ NOTE: Firebase has been migrated to Supabase
# These variables are kept for backward compatibility only
# You can remove them if you're not using Firebase anymore
#
# If you still need Firebase for migration purposes:
# 1. Go to Firebase Console: https://console.firebase.google.com/
# 2. Select your project → Project Settings (gear icon) → General tab
# 3. Copy the config values from your web app
#
# CLIENT-SIDE (deprecated):
# VITE_FIREBASE_API_KEY=your_firebase_api_key
# VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# VITE_FIREBASE_PROJECT_ID=your-project-id
# VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
# VITE_FIREBASE_APP_ID=your_app_id
# VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.region.firebasedatabase.app/
#
# SERVER-SIDE (deprecated):
# FIREBASE_API_KEY=your_firebase_api_key
# FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_STORAGE_BUCKET=your-project.appspot.com
# FIREBASE_MESSAGING_SENDER_ID=your_sender_id
# FIREBASE_APP_ID=your_app_id
# FIREBASE_DATABASE_URL=https://your-project-default-rtdb.region.firebasedatabase.app/
# FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account",...}'
